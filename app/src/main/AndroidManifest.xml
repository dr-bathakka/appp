<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools"/>
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools"/>
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions"
        xmlns:tools="http://schemas.android.com/tools"/>
    <uses-permission android:name="android.permission.MANAGE_MEDIA_PROJECTION" />

    <application
        android:name=".CluKeyApp"
        android:label="CluKey"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/Theme.CluKey"
        android:usesCleartextTraffic="true">

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AppLockActivity"
            android:exported="false"
            android:theme="@style/Theme.CluKey" />

        <!-- Settings -->
        <activity android:name=".ui.SettingsActivity"
            android:exported="false" android:theme="@style/Theme.CluKey" />
        <!-- App Usage / Screen Time -->
        <activity android:name=".ui.AppUsageActivity"
            android:exported="false" android:theme="@style/Theme.CluKey" />
        <!-- Panic Mode -->
        <activity android:name=".ui.PanicActivity"
            android:exported="false" android:theme="@style/Theme.CluKey" />

        <!-- Share Activity (phone ↔ PC ↔ server) -->
        <activity
            android:name=".ui.ShareActivity"
            android:exported="true"
            android:theme="@style/Theme.CluKey">
            <!-- Handle "Share" from any app -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Core Services -->
        <service
            android:name=".service.AssistantService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".service.VoiceService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".service.OverlayService"
            android:exported="false" />

        <!-- HTTP Bridge -->
        <service
            android:name=".network.HttpBridgeService"
            android:exported="false" />

        <!-- Notification Listener -->
        <service
            android:name=".service.NotificationMonitorService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Accessibility Service -->
        <service
            android:name=".service.ScreenReaderService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Location -->
        <service
            android:name=".service.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Device Health -->
        <service
            android:name=".service.DeviceHealthService"
            android:exported="false" />

        <!-- Intruder Detection -->
        <service
            android:name=".service.IntruderService"
            android:exported="false" />

        <service
            android:name=".service.IntruderCameraService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!-- SMS / Call Log -->
        <service
            android:name=".service.SmsCallLogService"
            android:exported="false" />

        <!-- App Usage -->
        <service
            android:name=".service.AppUsageService"
            android:exported="false" />

        <!-- Rules / Automation -->
        <service
            android:name=".service.RulesTickService"
            android:exported="false" />

        <!-- Long Poll (cloud commands) -->
        <service
            android:name=".service.LongPollService"
            android:exported="false" />

        <!-- Timer -->
        <service
            android:name=".service.TimerService"
            android:exported="false" />

        <!-- Focus Mode -->
        <service
            android:name=".service.FocusModeService"
            android:exported="false" />

        <!-- App Lock Accessibility Service -->
        <service
            android:name=".service.AppLockService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
        </service>

        <!-- Driving Mode -->
        <service
            android:name=".service.DrivingModeService"
            android:exported="false" />

        <!-- Message Sync -->
        <service
            android:name=".service.MessageSyncService"
            android:exported="false" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
